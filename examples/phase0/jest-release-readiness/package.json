{
  "name": "phase0-jest-release-readiness",
  "version": "1.0.0",
  "private": true,
  "description": "Realistic Jest adapter example for BrikByteOS Phase 0","scripts": {
    "pretest": "mkdir -p .tmp",
    "test": "jest --config ./jest.config.cjs --runInBand --json --outputFile ./.tmp/jest-report.json"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}